# ComposeUIComponents Agent Entry Point

Use `AGENTS.md` as the source of truth for agent instructions in this repo.

When repo guidance appears stale, prefer the accepted ADRs in `docs/adr/`, current docs pages under `docs/`, and the checked-in Gradle/module configuration over older references.

Repo workflows are available as skills in `.claude/skills/` (`new-component`, `review-component`, `verify-changes`).
